_build
dist
dist-newstyle
*~
*.hi
*.o
*.bak

# Local Claude Code agent definitions
.claude/

# Kept local, not version-controlled: the battle-test harness + its run
# artifacts (BattleTest/ — includes the PyRepr/ harness source and the
# generated per-tool reports/graphs) and the generated optimization HTML
# (OptSite/). The tracked test harness is .github/workflows/ci.yml.
BattleTest/
OptSite/

# Note: .agda-explore/deps.json IS tracked here as a query/test fixture
# (it is not this project's own generated graph — this repo links no Agda).
